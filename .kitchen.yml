driver:
  name: qemu

platforms:
  - name: x86_64
    driver:
      image_path: ./images
      image: disk.img
      username: ubuntu
      password: ubuntu
      hostshares:
        - path: shared
          mountpoint: /mnt/shared

suites:
  - name: vm
